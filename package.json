{
  "name": "agent-bridge",
  "version": "1.0.0",
  "description": "A tiny local MCP relay so two (or more) MCP-speaking agent CLIs — e.g. Claude Code and Grok Build — can post to and read from shared channels. The generalized cross-CLI bridge.",
  "type": "module",
  "private": true,
  "bin": {
    "agent-bridge": "server.js"
  },
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.8"
  }
}
